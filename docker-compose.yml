version: '4'

services:
  postgres-music:
    image: postgres:16.1
    environment:
      - POSTGRES_DB=music_db
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
    ports:
      - "5432:5432"